import { LevelProgressPipe } from './level-progress.pipe';

describe('LevelProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
